import { Container, Typography } from '@mui/material';

export const LoginPage = () => {
  return (
    <Container maxWidth="sm">
      <Typography variant="h1" sx={{ my: 2, fontSize: '2rem', fontWeight: 'bold' }}>Log In</Typography>
    </Container>
  );
};
