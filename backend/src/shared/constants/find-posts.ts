/** Select するカラム定義 */
export const selectColumns = [
  'posts.id', 'posts.userId', 'posts.text', 'posts.favouritesCount', 'posts.createdAt',
  'users.name', 'users.avatarUrl',
  'favourites.userId'
];
